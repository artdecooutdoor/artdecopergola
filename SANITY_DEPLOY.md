# 🚀 Инструкция по деплою Sanity Admin Panel

## Что было исправлено:

1. ✅ Удален старый `wrangler.toml` (он вызывал конфликты)
2. ✅ Остался только `wrangler.jsonc` для основного сайта
3. ✅ Создан `.sanityrc.json` для Sanity Studio

## Как развернуть админ-панель:

### Вариант 1: Встроенный Sanity Hosting (РЕКОМЕНДУЕТСЯ)

```bash
cd sanity
npm run deploy
```

Это разместит админ-панель на: **https://py6y7j4v.sanity.studio**

### Вариант 2: Локальная разработка админ-панели

```bash
cd sanity
npm run dev
```

Это откроет админ-панель на **http://localhost:3333**

## Структура после исправления:

```
artdeco/
├── wrangler.jsonc         ✅ Основной сайт (artdecopergola)
├── package.json
├── functions/
│   └── form.js           ✅ API функции
├── src/
│   ├── pages/
│   ├── components/
│   └── ...
├── sanity/
│   ├── package.json
│   ├── sanity.config.ts
│   ├── .sanityrc.json     ✅ Конфиг для Sanity Deploy
│   └── schemaTypes/
└── dist/                  ✅ Собранный основной сайт
```

## Процесс деплоя теперь:

1. **Основной сайт** → автоматически деплоится на Cloudflare Pages
2. **Админ-панель** → деплоится отдельно через `npm run deploy` в папке `sanity/`

## Если админ-панель не деплоится:

Убедитесь, что вы аутентифицированы в Sanity:

```bash
cd sanity
sanity login
sanity deploy
```

Вам выдадут URL админ-панели типа:
```
✓ Deployed to https://py6y7j4v.sanity.studio
```

## Проверка конфигурации:

```bash
# Проверить текущую конфигурацию Sanity
cd sanity
sanity config
```
